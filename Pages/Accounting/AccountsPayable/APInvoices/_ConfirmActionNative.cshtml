@model Vantage.PMS.Pages.Accounting.AccountsPayable.APInvoices.DetailsModel

<div class="vpms-native-workflow-card">
    <div class="vpms-native-workflow-kicker">AP control</div>
    <h2>@Model.NativeActionTitle</h2>
    <p>@Model.NativeActionMessage</p>

    <div class="vpms-native-workflow-summary">
        <span>AP Invoice</span>
        <strong>@Model.Invoice.InvoiceNumber</strong>
        <span>Supplier</span>
        <strong>@(Model.Invoice.Supplier?.SupplierName ?? "Not assigned")</strong>
        <span>Status</span>
        <strong>@Model.Invoice.Status</strong>
        <span>Balance</span>
        <strong>@Model.Invoice.Balance.ToString("C")</strong>
    </div>

    @if (!string.IsNullOrWhiteSpace(Model.NativeActionSupport))
    {
        <div class="vpms-native-workflow-note">@Model.NativeActionSupport</div>
    }

    <form method="post" asp-page="./Details" asp-page-handler="@Model.NativeActionHandler" asp-route-id="@Model.Invoice.Id" class="vpms-native-workflow-actions">
        <button type="submit" class="btn @Model.NativeActionButtonClass">@Model.NativeActionButtonText</button>
        <a href="#" class="btn vpms-btn-secondary" data-vpms-dialog-close>Keep Reviewing</a>
    </form>
</div>
